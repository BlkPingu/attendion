<!DOCTYPE html>
<html>
  <head>
    <title>Attendion</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track'=> 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track'=> 'reload' %>
  </head>

  <body>

  <%= render 'layouts/navigation' %>

        </ul>
      </div>
    </div>
    <p class="navbar-text pull-right">
      <% if user_signed_in? %>
        <%if current_user.name.present? %>
              <%= t ".logged_in_as" %> <strong><%= current_user.name %></strong> |
          <%=  link_to(t("layouts.application.logout_href"), destroy_user_session_path, method: :delete, :class => 'navbar-link') %>

    <% else %>
      <% end %>
        <% end %>
    </p>
  </nav>

    <div class="container">
    <%= yield %>

      <p class="navbar-text pull-right">
        <% if user_signed_in? %>
        <% else %>
          <li><%=  link_to(t(".signup_href"), new_user_registration_path) %></li> |
          <li><%=  link_to(t(".login_href"), new_user_session_path) %></li>
        <% end %>
      </p>
    </div>
  </body>
</html>
