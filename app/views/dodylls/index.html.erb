<% if @dodylls.blank? %>
  <p>You don't have any Dodylls.</p>
<% else %>
  <p>These are the current Dodylls</p>
  <ul id = "dodylls">
    <% @dodylls.each do |c| %>
      <td><%= link_to 'Show', dodyll_path(dodyll) %></td>
    <% end %>
  </ul>

<% end %>
<p><%= link_to "Create new Dodyll", {:action => 'new' }%></p>